letras = ["x","y","z"]

# podemos recorrer la lista usando for
for letra in letras:
    print(letra)

# mediante range con for
for indice in range(0, len(letras)):
    valor_lista = letras[indice]
    print(valor_lista)